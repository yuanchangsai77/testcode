# Project Principles

- 无感顺利：安全与恢复尽量由运行时自动完成，不打断用户，不无故增加模型回合。
- 模块化、可维护：职责单一、边界清晰、实现简单，并用测试保护关键行为。
- 接口复用：优先扩展现有契约和公共能力，避免重复实现与兼容旁路。
- 文档先行：设计或契约变化先更新文档；架构、核心机制、接口参考和版本记录各司其职，避免重复。
- 本地模型测试访问 `127.0.0.1` 时必须绕过代理变量；Agent 与用户终端结果不一致时先检查代理环境。
